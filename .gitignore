# Build output
dist/
bin/
*.exe

# Compiled binaries (build locally with go build)
qubicdb
qubicdb-cli

# Vector wrapper build artifacts (generated by patches/vector-wrapper/build.sh)
patches/vector-wrapper/_build/

# Runtime shared libraries (built locally, not committed)
*.dylib
*.so
*.dll

# Go test cache / coverage
*.test
*.out
coverage.html

# Data files (runtime state)
*.nrdb
data/

# Local config (may contain secrets)
qubicdb.yaml

# Docs (maintained in github.com/qubicDB/docs)
docs/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
